using eindprojectGameDev.interfaces;
using eindprojectGameDev.Map;
using Microsoft.Xna.Framework;
using Microsoft.Xna.Framework.Graphics;
using System;
using System.Collections.Generic;
using System.Diagnostics.CodeAnalysis;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace eindprojectGameDev.World
{
    internal class Drawer
    {
        /*
        Level1 level1;
        bool initialized = false;
        Level2 level2 = null;
        GameStates previousState = GameStates.menu;
        LevelManager levelManager = new LevelManager();
        GameServiceContainer Service;
        Start start = null;
        public Drawer()
        {
            levelManager.Initialize();
            Service = Game1.Instance.Services;
            Service.AddService<LevelManager>(levelManager);
            start = new Start();
            level1 = new Level1();
            levelManager.ChangeSet(level1.blockArray);

        }

        public void Update(GameTime gameTime)
        {
            switch (GameState.gameState)
            {
                case GameStates.menu:
                    start.Update(gameTime);
                    break;
                case GameStates.level1:
                    level1.Update(gameTime);
                    break;
                case GameStates.level2:
                    break;
                case GameStates.gameover:
                    break;
                case GameStates.exit:
                    break;
                default:
                    break;
            }
            //previousState = ;
        }

        public void Draw(SpriteBatch spriteBatch)
        {
            switch (GameState.gameState)
            {
                case GameStates.menu:
                    start.Draw(spriteBatch);
                    break;
                case GameStates.level1:
                    level1.Draw(spriteBatch);
                    break;
                case GameStates.level2:
                    break;
                case GameStates.gameover:
                    break;
                case GameStates.exit:
                    break;
                default:
                    break;
            }
        }

        */
    }
}
